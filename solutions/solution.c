/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
#include <cstddef>
struct ListNode* mergeTwoLists(struct ListNode* list1, struct ListNode* list2) {
    return NULL; // Replace with merging logic later
}
